import { switching } from 'frontend/helpers/switching';
import { module, test } from 'qunit';

module('Unit | Helper | switching');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = switching([42]);
  assert.ok(result);
});
